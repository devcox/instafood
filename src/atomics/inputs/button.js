import React from 'react';


const Button = ({
    id,
    name,
    classCSS,
    event
}) => {
    return (
        <>
            <button>{name}</button>
        </>
    )
}

export default Button;
